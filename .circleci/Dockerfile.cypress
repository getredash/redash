FROM cypress/browsers:node14.0.0-chrome84

ENV APP /usr/src/app
WORKDIR $APP

COPY package.json yarn.lock .yarnrc $APP/
COPY viz-lib $APP/viz-lib
RUN npm install yarn@1.22.10 -g && yarn --frozen-lockfile > /dev/null

COPY . $APP

RUN ./node_modules/.bin/cypress verify
