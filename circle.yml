machine:
  node:
    version:
      0.10.22
dependencies:
  pre:
    - make deps
    - pip install requests
test:
  post:
    - make pack 
deployment:
  github:
    branch: master
    commands:
      - make upload
