/* eslint-disable global-require, import/no-unresolved */
// import prepareData from './prepareData';

describe('Visualizations', () => {
  describe('Chart', () => {
    describe('prepareData', () => {
      test('Pie: single series', () => {
        // Stub
      });
      test('Pie: multiple series', () => {
        // Stub
      });

      test('Heatmap', () => {
        // Stub
      });

      test('Default: single series', () => {
        // Stub
      });

      test('Default: multiple series of single type', () => {
        // Stub
      });

      test('Default: multiple series of all types', () => {
        // Stub
      });
    });
  });
});
