import React from 'react';
import { EditorPropTypes } from '@/visualizations';

export default function DefaultColorsSettings() {
  return (
    <div>Default Colors Settings</div>
  );
}

DefaultColorsSettings.propTypes = EditorPropTypes;
