import React from 'react';
import { EditorPropTypes } from '@/visualizations';

export default function DataLabelsSettings() {
  return (
    <div>Data Labels Settings</div>
  );
}

DataLabelsSettings.propTypes = EditorPropTypes;
