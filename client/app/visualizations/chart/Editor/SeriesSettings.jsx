import React from 'react';
import { EditorPropTypes } from '@/visualizations';

export default function SeriesSettings() {
  return (
    <div>Series Settings</div>
  );
}

SeriesSettings.propTypes = EditorPropTypes;
