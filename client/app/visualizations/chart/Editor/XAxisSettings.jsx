import React from 'react';
import { EditorPropTypes } from '@/visualizations';

export default function XAxisSettings() {
  return (
    <div>X Axis Settings</div>
  );
}

XAxisSettings.propTypes = EditorPropTypes;
