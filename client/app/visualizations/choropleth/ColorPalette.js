import { extend } from "lodash";
import ColorPalette from "@/visualizations/ColorPalette";

export default extend(
  {
    White: "#ffffff",
    Black: "#000000",
    "Light Gray": "#dddddd",
  },
  ColorPalette
);
